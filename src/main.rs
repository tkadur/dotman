#[allow(dead_code, unused_variables)]

mod config;

fn main() {
    // match rcrc::get() {
    //     Ok(config) => println!("{:?}", config),
    //     Err(error) => println!("Could not load .rcrc\n{}", error),
    // }
}
